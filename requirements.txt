# One-shot setup on any machine (from the repo root, inside your env):
#
#     pip install -r requirements.txt
#
# Installs the mlr package in editable mode plus dev tools (pytest).
# Dependencies are defined once, in pyproject.toml — this file just points there,
# so it can never drift out of sync.
-e .[dev]

# Working with PyTorch models too? Use this instead:
#     pip install -e .[dev,torch]
