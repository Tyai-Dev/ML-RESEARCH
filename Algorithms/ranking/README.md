# Ranking

Planned: ordinal/ranking models, starting with PRank (Crammer-Singer) - a single weight vector plus K-1 ordered thresholds, classifying by where the projection w.x lands; perceptron-style mistake bound; the threshold-sliding animation.

